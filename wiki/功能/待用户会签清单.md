---
name: 待用户会签清单
type: requirements-signoff
status: open
---

# 待用户会签清单

本页只收集需求源之间已经确认存在演化、但不能由需求分析者自行改写的事项。用户会签前，原文保留，功能页以“需求已演化待换代”标记。

| 编号 | 功能域 | 原需求 | 新现实/新需求 | 需要用户裁定 |
|---|---|---|---|---|
| S-001 | F1/F3 | `team-agent-restart-foundation-goal.md` §5、§8：session 缺失时走 fresh spawn 并 warning | 当前恢复/分叉纪律要求 backing 未形成时诚实 pending，无法证明时 rejected/fail closed；fresh 会改变上下文 | fresh 是否必须由用户/leader 显式同意；若同意，哪些入口允许默认 fresh |
| S-002 | F2 | `team-agent-skill-hardening-goal.md` §3/§5 与早期 goal：worker 不得 nested Team Agent | 后续需求已把有界 team-in-team 作为责任下放能力，并已有深度/资源边界方向 | 正式废止永久禁令，还是保留默认禁止、仅 leader 显式授权时允许；旧 goal 如何标记 superseded |
| S-003 | F3 | 基础边界 §11：必须支持列举的跨厂商组合 | 0.5.57 有多 provider 与三个订阅场景证据，但没有完整 leader/worker/compatible API 矩阵 | “完成”按能力分级诚实披露，还是要求一张固定跨厂商真机矩阵全部通过 |
| S-004 | F4/F5 | 基础边界 §14 与 quick-start goal：以 capture/token 的 `visible` 作为短期送达承诺 | 风暴与假阴案例要求把物理 attempt、屏幕回显和 provider 对话 receipt 分开，只有 receipt 可终结 obligation。2026-08-18 派单给定实测（本席未另测）：投递路径会验也会重试（最多 3 次、只重按回车），三次用尽后停在 `injected_awaiting_receipt`；当天 73 条：`delivered` 33、`injected_awaiting_receipt` 37、`queued_pane_missing` 2、`submitted_unverified` 1。copy-mode 能被识别且退出能力在，但投递路径无调用方。此为同一「注入→收据」缺口的新现实，不另开号。 | 正式把 delivered 定义换代为 provider 对话 receipt；`visible` 保留为观察状态还是废止。连带：receipt 迟迟不来时，义务是停在 `injected_awaiting_receipt` 等待，还是框架必须继续推进（含 copy-mode 应否由投递路径主动退出） |
| S-005 | F5 | quick-start goal：按超时/弱状态推断 worker stuck 并主动推 leader | 后续准则倾向禁止框架基于时间、屏幕内容或弱状态主动 nag，只保留强双信号异常与人工确认 | stuck/idle 主动提醒是否仍是产品承诺；若保留，最低强信号与用户可配置边界是什么 |
| S-006 | F6 | skill-hardening goal 固定早期 `result_envelope_v1` 与 collection item 形状 | 当前结果信封、presentation 与 tests 归一化已有新字段；0.5.57 还出现真实执行证据被误判 `not_run` | 指定唯一合法 result/tests schema，以及未知但结构化证据应拒绝、保留还是兼容归一化 |
| S-007 | F8 | skill-hardening goal 要求用户可调用 task-scoped `repair-state` | 后续方向是中间状态由 runtime 自愈，用户/agent 只管理第一真相源，避免手工修复派生状态 | `repair-state` 应保留为高级显式工具，还是从用户产品面删除并完全内部化 |
| S-008 | F9 | 基础边界 §6 长期要求 external leader 不被 tmux 绑定；后续已有 managed launcher | 两种拓扑均有合法场景，但默认入口与长期产品叙事尚未在需求源统一 | managed launcher 是否为默认、external attach 是否永久保留、两者验收权重如何分配 |
| S-009 | F9 | 基础边界 §16 倾向 Ghostty 原生可见窗口 | 当前产品同时支持 headless/private tmux 与多种显示 backend | 默认显示模式是什么；Ghostty 是推荐 view、默认控制面还是仅可选 backend |
| S-011 | F4/F5/F6 | 0.5.54–0.5.57 的 stage_result/stage_pass/bounce/final_review 在 send/presentation 平面承载流水线状态，并已有冻结 msg-routing 契约 | 0.5.60 正式需求把 stage_* 语义搬到 [[result_route 与流水线结果消费]]，强制面改锚 [[typed 状态事实目录与强制穿透]]；[[send 单信箱与状态事实穿透]] 建议普通旧 class 保留一个小版本兼容期，send 侧 stage 类在后片就绪后日落 | 旧 stage_* 字段是立即拒绝、限期兼容读取还是只读迁移；兼容窗口与旧冻结契约的终止点如何定义 |

## 会签规则

1. 用户裁定后，在本页记录日期、裁定原文与受影响功能页。
2. 先更新功能 wiki 的承诺与黑盒验收，再另开源需求换代；不得只改实现或测试。
3. 未会签项不阻止记录 0.5.57 现状，但不得被写成新的正式需求。

## 已会签 · Pi provider 适配（2026-08-31）

**裁定原文位置**：`.team/artifacts/pi-provider-adaptation/requirements/pi-provider-requirement-final.md`（用户已会签，可供需求维基管理员入库）。

**受影响功能页**：[[Pi provider 适配]]（条款地图）、[[F1 对话开团与团队生命周期]]、[[F2 角色、成员与能力编排]]、[[F3 Provider、模型、权限与资源组合]]、[[F7 会话身份、续作、克隆与分叉]]、[[F9 显示、平台、安装与黑盒体验]]、[[F10 治理边界与黑盒验收]]。

**本会签闭合的 Pi 专属边界**（不关闭下列通用会签项）：

- restart/resume 只在持久 backing 存在且身份匹配时恢复原会话；缺失或不匹配时 fail-closed；start-agent reconstruction 不得冒充 resume；reset 为丢弃旧会话并建立 fresh。S-001 的通用 fresh 入口裁定仍开放。
- “支持普通 tmux pane”不等于要求用户管理 tmux；用户无需理解或操作底层 tmux socket。S-008 的 managed/external 默认入口裁定仍开放。
- 不为 Pi 建立新的通用 Provider schema；共享字段仍以现有角色需求为权威。S-003 的跨厂商矩阵完成标准仍开放。

## ~~待会签~~ **已撤回（2026-08-18，leader 误立）** · F5「Working leader 不打断」与 send 及时性

🔴 **本条不需要用户会签，是我（leader）把一处文档补句错立成了契约裁定。**
用户已在 2026-08-18 裁定完毕：**`send` 参数化，及时/非及时由调用方 agent 自己选**。
⇒ F5 剩下的事只是**补一句措辞**：「及时投递会打断当前回合（grok/cursor 实测）」，
由 leader 直接改档，不占用户裁定。**ESC 与本条无关**（ESC 是用户举的例子，已废，红线保留）。

以下原文保留作记录：

**冲突双方**
- **F5 现文**（`wiki/功能/F5 Leader 被动收信与呈现.md`）：Working 中的 leader/席位**不被打断**。
- **用户裁定（2026-08-18）**：`send` 增加「及时 / 非及时」参数，**由调用方 agent 判断**；
  grok **必须按回车顶出队列**（及时投递），而**顶出去实测会打断当前回合**
  （grok `outcome=cancelled`；cursor 同形，长回合被丢弃）。
  裁定同时明确：**provider 的队列不作为编排手段**（它把消息变成目标流水线，
  而我们已有 `report_result` 且节点可分批交接）。

**发现者**：验证种系 `tester-t80`（**标"待补认"并拒绝自裁改 F 页——处理正确，契约变更不是席位权限**）。

**要会签的是什么**（一句话）：**"不打断"从不变量降级为默认值，还是保留为不变量而让及时投递成为例外？**
- 若"不打断"仍是不变量 ⇒ 及时投递只能用于救济类消息，F5 需写明例外清单。
- 若改为默认值 ⇒ F5 现文要改，且要写清**被打断的那一轮里已完成的工具调用如何处置**（目前未量过）。

⚠️ **未会签之前**：`ledger.send-timeliness` 的判据按**用户裁定的语义**断言，
并在证据里自标「与 F5 现文冲突，以会签结果为准」。
